<!-- _includes/partials/logo-cloud.liquid -->
<img
  class="col-span-{{ col_span_sm }} max-h-[{{ logo_img_height }}] w-full object-contain lg:col-span-{{ col_span_lg }}"
  src="{{ logo_img }}"
  alt="{{ logo_alt_tag }}"
  {% imageDimensions logo_img %}
>