<div class="container-lg flex flex-col gap-10 bg-teafc-blue px-8 lg:my-20">
  <div class="mt-8 flex flex-col gap-2 md:mt-4 lg:mt-8">
    <h1 class="text-4xl font-semibold text-white">{{ current-events.current_event_header | upcase }}</h1>
    <p class="text-lg font-light text-white">
      {{ current-events.current_event_text -}}
      {{- current-events.current_event_text }}
    </p>
  </div>

  <div class="mx-6 mb-10 flex flex-col gap-4 md:mx-0 md:flex-row md:gap-6 lg:gap-8">
    <div class="basis-5/12 rounded-2xl bg-red-600">
      <img
        class="h-full w-full rounded-2xl object-cover"
        src="{{ current-events.event_1_img }}"
        alt=""
      >
    </div>
    <div class="flex basis-7/12 flex-col gap-8 sm:gap-4">
      <h2 class="pb text-3xl font-semibold text-white">{{ current-events.event_1_title }}</h2>
      <p class="font-light text-white">{{ current-events.event_1_text }}</p>
    </div>
  </div>
</div>
